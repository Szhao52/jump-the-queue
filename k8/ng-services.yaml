apiVersion: v1
kind: Service
metadata:
  name: angular-service
spec:
  type: NodePort
  selector:
    name: angular-app
    app: angular-app
  ports:
    - protocol: TCP
      port: 80
      nodePort: 30080

pipeline {
    agent any
    stages{
        stage('Build Angular'){
            steps{
                dir('angular'){
                    sh 'docker build -t angular-app'
                    sh 'docker images'
                }
            }
        }
    }
}
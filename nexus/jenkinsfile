pipeline {
    agent any
    stages{
        stage('Build Angular'){
            steps{
                script{
                    sh 'docker build -t angular-app ./angular'
                    sh 'docker images'
                }
            }
        }
    }
}
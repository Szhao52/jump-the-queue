pipeline {
    agent any
    stages{
        stage('Build Angular'){
            steps{
                
                    sh 'docker build -t angular-app /angular'
                    sh 'docker images'
                
            }
        }
    }
}
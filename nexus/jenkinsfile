pipeline {
    agent any
    stages{
        steps{
            stage('Build Angular'){
                dir('angular'){
                    steps{
                        sh 'docker build -t angular-app'
                        sh 'docker images'
                    }
                }
            }
        }
    }
}